{
    "layer": "bottom",
    "position": "top",
    "height": 39,
    "modules-left": ["hyprland/workspaces", "tray"],
    "modules-center": ["clock"],
    "modules-right": ["cpu", "temperature", "pulseaudio", "custom/notifications"],

    "hyprland/workspaces": {
        "format": "{id}",

    },

    "clock": {
        "format": "{:%H:%M:%S}",
        "tooltip-format": "{:%A, %d %B %Y}",
	"interval": 1
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "",
        "format-icons": ["", "", ""],
	"on-click": "~/.config/waybar/scripts/volume-toggle.sh" 
   },

    "custom/notifications": {
	"format": "󰂚",
	"on-click": "swaync-client -t"
    },

    "tray": {
        "spacing": 10
    },

    "cpu": {
        "interval": 2,
        "format": "󰍛 {usage}%",
        "max-length": 10
    },

    "temperature": {
        "format": "󰍛 {temperatureC}°C",
        "critical-threshold": 80,
        "tooltip": false
   }
}
